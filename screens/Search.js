import React from "react";
import { View, Text } from "react-native";

const Search = ({ navigation }) => {
  return (
    <View>
      <Text style={{ paddingTop: 200, textAlign: "center" }}>Search</Text>
    </View>
  );
};

export default Search;
