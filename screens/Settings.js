import React from "react";
import { View, Text } from "react-native";

const Settings = ({ navigation }) => {
  return (
    <View>
      <Text style={{ paddingTop: 200, textAlign: "center" }}>Settings</Text>
    </View>
  );
};

export default Settings;
